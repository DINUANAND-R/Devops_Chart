apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: nexchat
  labels:
    app: nginx
spec:
  selector:
    app: nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080   # Access via http://localhost:30080 on Minikube
  type: NodePort
